on: pull_request

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

  approve-dependabot:
    needs: checkout
    uses: ./.github/workflows/approve_dependabot.yml
    permissions:
      pull-requests: write
      contents: write
