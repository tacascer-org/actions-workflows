on: pull_request

jobs:
  approve-dependabot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/approve_dependabot.yml
    permissions:
      pull-requests: write
      contents: write
